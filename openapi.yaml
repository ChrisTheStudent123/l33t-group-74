openapi: 3.0.3
info:
  title: Game Collection API
  version: 1.0.0

servers:
    - url: http://localhost:3000

paths:
    /games:
      get:
        summary: List of games
        responses:
          "200":
            description: A list of games