{
  "name": "l33t-group-74",
  "module": "server.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "cross-env": "^10.1.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "start": "bun src/server.ts",
    "testing": "cross-env RATE_LIMIT=off bun src/server.ts",
    "dev": "cross-env RATE_LIMIT=on bun --watch src/server.ts",
    "dev:testing": "cross-env RATE_LIMIT=off bun --watch src/server.ts"
  },
  "dependencies": {
    "rate-limiter-flexible": "^10.0.1",
    "redoc": "^2.5.2"
  }
}
